import 'package:flutter/material.dart';

class WButton extends StatelessWidget {
  const WButton({Key? key, required this.child, required this.onTap})
      : super(key: key);

  final Widget child;
  final GestureTapCallback onTap;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
